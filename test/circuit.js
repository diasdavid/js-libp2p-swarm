/* eslint-env mocha */
'use strict'

const chai = require('chai')
const dirtyChai = require('dirty-chai')
const expect = chai.expect
chai.use(dirtyChai)

describe(`circuit`, function () {
  it(`should enable circuit`, function () {

  })

  it(`should dial circuit last`, function () {

  })

  it(`should not dial over circuit`, function () {

  })
})
